ja:
  activerecord:
    errors:
      models:
        post:
          attributes:
            base:
              content_body_attachables_byte_size_is_too_big: 添付ファイルは%{max_content_attachment_mega_size}MB以下にしてください。[%{bytes}/%{max_bytes}]
            title:
              blank: が空です。
              too_long: が%{count}文字を超えています。
            content:
              too_long: が%{max_content_length}文字を超えています。[%{length}/%{max_content_length}]
              attachable_count_is_too_big: の添付画像の数が%{max_content_body_attachables_count}個を超えています。
    attributes:
      post:
        title: タイトル
        content: コンテンツ
